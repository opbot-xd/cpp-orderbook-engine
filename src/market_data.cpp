#include "matching/market_data.h"

// Market data handler is mostly header-only (template callbacks and inline methods).
// This TU exists so the build system has a concrete translation unit.

namespace matching {
// intentionally empty
} // namespace matching
