#include "matching/spsc_queue.h"

// SPSC queue is a header-only template. This TU exists so the build
// system has a concrete translation unit for it.

namespace matching {
// intentionally empty — all code is in the header
} // namespace matching
